# Tokens are distinct; generate long random values and never commit the real values.
BROWSER_WORKER_TOKEN=
CAR_PART_WORKER_URL=
CAR_PART_WORKER_TOKEN=
MANUAL_RUN_TOKEN=
# Browserless shared-cloud API key for the optional remote-CDP test.
BROWSERLESS_API_KEY=
CONSOLE_USERNAME=
CONSOLE_PASSWORD=
# DATABASE_URL=postgresql://...
# BROWSERLESS_ENDPOINT=wss://production-sfo.browserless.io
